// game.js
// construction object
var Game = function() {



  
};

// write constructor function. board, player x, playero, next turn.( or will it be placed into thing above.)

Game.prototype = {
// tested this one was connected, tested!
helloWorld: function() {return "hello world";},

game: function(spot) {

    var playerX = playerX; // or would this be where we set it to "Frida" and "Harry"?
    var playerO = playerO;

    var board = [
    ["undefined", "undefined", "undefined"],
    ["undefined", "undefined", "undefined"],
    ["undefined", "undefined", "undefined"]
  ];
  // or here as it's servering as  a counter and should be contained within game.


},
nextTurn: function() {
  // will it go here as an action?
  // create counter
  // if nextTurn starts 0. or will playerX always be zero?


  }

};









export default Game;
// module.exports = Game; // do we need this?
